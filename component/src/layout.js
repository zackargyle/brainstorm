import React, { PropTypes } from 'react';

import './style.scss';

const {{ COMPONENT_NAME }}Layout = ({}) => (
    <div className="{{ COMPONENT_NAME }}">
        Start building {{ COMPONENT_NAME }}!
    </div>
);

{{ COMPONENT_NAME }}Layout.propTypes = {

};

{{ COMPONENT_NAME }}Layout.defaultProps = {

};

export default {{ COMPONENT_NAME }}Layout;
